import _ from 'lodash';
import Base from '../../base';

const state = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default _.merge({}, Base, {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
});
